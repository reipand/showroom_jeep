version: '3.8'
services:
  web:
  
    image: php:8.2-apache 
    container_name: php-webserver
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
    
    user: "1000:1000"
